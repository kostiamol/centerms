language: go
go: 
 - "1.12.x"
os:
    - linux
dist: trusty
sudo: false
env:
    - GO111MODULE=on
install: true
matrix:
    fast_finish: true
notifications:
    email: false
before_script:
    - GO_FILES=$(find . -iname '*.go' -type f | grep -v /vendor/)
    - go get -mod=vendor -u golang.org/x/lint/golint
    - go get -mod=vendor -u honnef.co/go/tools/cmd/megacheck
    - go get -mod=vendor -u github.com/fzipp/gocyclo
    - go get -mod=vendor -u golang.org/x/tools/cmd/cover
    - go get -mod=vendor -u github.com/mattn/goveralls
script:
    - go test -mod=vendor -v -race ./...
    - go vet -mod=vendor ./...
    - gocyclo -over 19 $GO_FILES
    - golint -set_exit_status $(go list ./...)
    - "$GOPATH/bin/goveralls -service=travis-ci"
